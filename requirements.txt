# Core dependencies
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0  # Required for confusion matrix in dashboard

# Web dashboard (required for Streamlit Cloud)
streamlit>=1.28.0
plotly>=5.17.0

# Database
# SQLite is included in Python standard library

# Optional: ML dependencies (only needed for training, not for dashboard)
# Uncomment if you need to train models locally:
# xgboost>=2.0.0
# joblib>=1.3.0

# Optional: Web scraping (only needed for local scraping, not for dashboard)
# Uncomment if you need to scrape locally:
# selenium>=4.15.0
# webdriver-manager>=4.0.0
# beautifulsoup4>=4.12.0

# Utilities
python-dateutil>=2.8.0

